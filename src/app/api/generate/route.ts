import { NextResponse } from "next/server";

const POLZA_URL = "https://polza.ai/api/v1/media";
const MODEL = "google/gemini-3.1-flash-image";

export async function POST(request: Request) {
  try {
    const { theme, words, grade, difficulty, style } = await request.json();

    if (!theme?.trim() || !Array.isArray(words) || words.length === 0) {
      return NextResponse.json(
        { error: "Укажите тему и хотя бы одно словарное слово." },
        { status: 400 }
      );
    }

    if (words.length > 12) {
      return NextResponse.json(
        { error: "Можно добавить не более 12 слов." },
        { status: 400 }
      );
    }

    const apiKey = process.env.POLZA_API_KEY;

    if (!apiKey) {
      return NextResponse.json(
        { error: "Не настроен POLZA_API_KEY." },
        { status: 500 }
      );
    }

    const targetWords = words
      .slice(0, 12)
      .map((word: string, index: number) => `${index + 1}. ${word}`)
      .join("\n");

    const prompt = `
СОЗДАЙ ОДНУ ЦЕЛЬНУЮ УЧЕБНУЮ ИЛЛЮСТРАЦИЮ
для школьной карточки по русскому языку
для детей ${grade || 2} класса.

ЦЕЛЬ:
Ребёнок 7–10 лет должен внимательно рассмотреть картинку
и найти в ней конкретные объекты, соответствующие заданным
словарным словам.

Картинка должна быть красивой, доброй, понятной ребёнку
и выглядеть как иллюстрация из качественной классической
детской книги.

ТЕМА СЦЕНЫ:
${theme.trim()}

СЛОВАРНЫЕ СЛОВА ДЛЯ ПОИСКА:
${targetWords}

ВАЖНО:
Количество слов может быть от 1 до 12.
Нужно изобразить ВСЕ заданные объекты.
Каждый заданный объект должен присутствовать РОВНО ОДИН РАЗ.

==================================================
ГЛАВНЫЙ ПРИНЦИП — ЛОГИКА СЦЕНЫ
==================================================

Сначала придумай естественную и логичную сцену по теме,
а затем органично включи в неё все заданные объекты.

Целевые объекты не должны выглядеть так,
будто их специально разложили по картинке
только ради задания.

Каждый объект должен иметь понятную причину
находиться именно в этом месте.

Предметы должны использоваться по своему обычному назначению
или естественно находиться в соответствующей обстановке.

Например:
- пальто должно быть надето на человеке
  или аккуратно висеть на вешалке, крючке, спинке стула,
  скамейке или другом естественном месте,
  если это соответствует ситуации, но естественное, а не так как-будто его разложили, должны быть складки, естественная небрежность;
- тетрадь может лежать на парте,
  находиться в руках ребёнка или быть раскрыта рядом с учеником;
- собака может гулять рядом с человеком,
  сидеть возле хозяина или спокойно лежать в подходящем месте;
- учитель должен находиться в естественной школьной ситуации;
- ученик должен выглядеть как ученик и находиться
  в соответствующей обстановке;
- предметы одежды не должны выглядеть случайно брошенными
  или забытыми;
- предметы не должны висеть или лежать в странных местах
  только ради того, чтобы их было видно.

Если слово обозначает человека,
человек должен естественно участвовать в происходящем.

Если слово обозначает животное,
животное должно находиться в естественной для него ситуации.

Если слово обозначает предмет,
предмет должен находиться там, где он действительно
мог бы оказаться в реальной жизни.

==================================================
СЮЖЕТ И КОМПОЗИЦИЯ
==================================================

Создай одну большую цельную сцену,
а не набор отдельных объектов.

У сцены должна быть понятная внутренняя история:
ребёнок должен сразу понимать,
что здесь происходит.

Сцена может происходить, например:
в школе,
на школьном дворе,
в парке,
на улице,
дома,
в деревне,
на вокзале,
в магазине,
в библиотеке,
на прогулке,
на природе
или в любом другом месте,
которое естественно соответствует теме.

Не ограничивайся лесом или парком.
Тема может быть любой.

Используй естественную глубину пространства:
передний план,
средний план,
задний план.

Целевые объекты распределяй по сцене естественно.

Не ставь целевые объекты рядом друг с другом.

Не выстраивай их в ряд.

Не делай симметричную композицию.

Не помещай все целевые объекты в центр.

Некоторые объекты могут находиться ближе,
другие дальше,
некоторые — сбоку,
некоторые — частично окружены элементами среды.

Но каждый объект должен оставаться достаточно хорошо видимым,
чтобы ребёнок мог его найти.

==================================================
ПОИСКОВАЯ СЛОЖНОСТЬ
==================================================

Сложность: ${difficulty || "Средняя"}.

Сложность должна достигаться не за счёт хаоса,
а за счёт естественного расположения объектов.

Простая:
объекты хорошо заметны.

Средняя:
часть объектов немного спрятана
среди естественных деталей сцены.

Сложная:
объекты могут находиться дальше,
сбоку, частично прикрываться ветками,
листьями, мебелью или другими элементами среды,
но всё равно должны быть однозначно узнаваемыми.

Никогда не скрывай объект полностью.

Не делай целевые объекты крошечными.

==================================================
ОБЯЗАТЕЛЬНЫЕ ОБЪЕКТЫ
==================================================

Каждое слово из списка означает ОДИН конкретный объект.

Каждый объект:
- присутствует ровно один раз;
- легко узнаваем;
- соответствует именно заданному слову;
- имеет естественную форму;
- имеет правильные пропорции;
- находится в логичном месте;
- не является игрушкой;
- не является статуей;
- не является рисунком;
- не изображён на плакате;
- не изображён на вывеске;
- не является фотографией внутри изображения.

Не заменяй объект похожим объектом.

Если задано слово «собака»,
должна быть именно одна настоящая собака,
а не игрушечная собака или рисунок собаки.

Если задано слово «пальто»,
должно быть именно одно пальто,
естественно существующее в сцене.

==================================================
ОЧЕНЬ ВАЖНО ПРИ БОЛЬШОМ КОЛИЧЕСТВЕ СЛОВ
==================================================

Если задано 8–12 слов,
не пытайся сделать 12 объектов одинаково крупными.

Создай насыщенную, но логичную сцену,
в которой разные объекты находятся
на разных планах и в разных местах.

Дополнительные элементы сцены разрешены,
если они помогают сделать сцену естественной.

Но не добавляй дополнительные экземпляры
целевых объектов.

Не добавляй другие объекты того же типа,
если они могут запутать ребёнка.

==================================================
ВИЗУАЛЬНАЯ ЛОГИКА
==================================================

Каждый объект должен быть частью общей истории.

Не создавай ощущение:
«Вот объект №1, вот объект №2, вот объект №3».

Вместо этого создай ощущение настоящего места,
где всё происходит естественно,
а ребёнку предлагается внимательно рассмотреть
это место и обнаружить нужные слова.

Например, если тема — школьный двор,
дети могут идти после уроков,
учитель может разговаривать с учениками,
кто-то может сидеть на скамейке,
кто-то играть,
на скамейке может естественно лежать школьная вещь,
рядом может быть собака с хозяином.

Все элементы должны взаимодействовать
с окружающей средой.

==================================================
СТИЛЬ
==================================================

Стиль:
${style || "Реалистичная иллюстрация"}.

Приоритет:
традиционная качественная иллюстрация
для детской книги.

Предпочтительная техника:
акварель и гуашь,
мягкая живописная фактура,
естественные цвета,
тёплая спокойная палитра.

Атмосфера:
добрая,
уютная,
живая,
немного сказочная,
как качественная классическая детская книга.

Рисунок выразительный,
но реалистичный по формам.

Не фотография.

Не фотореализм.

Не 3D.

Не CGI.

Не современный глянцевый мультфильм.

Не плоская компьютерная графика.

==================================================
ЛЮДИ И ЖИВОТНЫЕ
==================================================

Правильная анатомия.

Естественные позы.

Нормальные пропорции.

У людей:
нормальное количество рук,
ног,
пальцев.

У животных:
правильное количество лап,
глаз,
ушей,
хвостов,
крыльев.

Никаких анатомических ошибок,
слишком длинных конечностей,
лишних частей тела,
слияния людей или животных друг с другом.

==================================================
ЧИСТОТА ИЗОБРАЖЕНИЯ
==================================================

СТРОГО НЕ ДОБАВЛЯЙ:

текст,
буквы,
цифры,
слова,
подписи,
надписи,
вывески,
таблички,
логотипы,
водяные знаки.

Не добавляй написанные слова
даже если они могли бы естественно появиться
на школьной доске, книге, тетради или вывеске.

Не создавай:
рамки,
панели,
коллаж,
несколько отдельных сцен,
разделение изображения на части.

Это должна быть ОДНА цельная иллюстрация.

==================================================
ФИНАЛЬНАЯ ПРОВЕРКА ПЕРЕД ГЕНЕРАЦИЕЙ
==================================================

Перед созданием изображения проверь:

1. Все заданные слова представлены объектами.
2. Каждый целевой объект присутствует ровно один раз.
3. Ни один объект не заменён похожим.
4. Все объекты логично вписаны в сюжет.
5. Ни один предмет не выглядит случайно брошенным.
6. Одежда находится на человеке или в естественном месте.
7. Люди и животные выглядят анатомически правильно.
8. Объекты достаточно хорошо видны для ребёнка.
9. Нет лишних экземпляров целевых объектов.
10. Нет текста и надписей.
11. Сцена выглядит как единое реальное место,
    а не как каталог предметов.

ФОРМАТ:
Одна цельная горизонтальная иллюстрация.
Соотношение сторон 16:9.
Подходит для печати на школьной карточке A5–A4.
`;

    const response = await fetch(POLZA_URL, {
      method: "POST",
      headers: {
        Authorization: `Bearer ${apiKey}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: MODEL,
        input: {
          prompt,
          aspect_ratio: "16:9",
          max_images: 1,
        },
      }),
    });

    const data = await response.json();

    if (!response.ok) {
      console.error("Polza API error:", data);

      return NextResponse.json(
        {
          error:
            data?.error?.message ||
            data?.message ||
            "Polza не приняла запрос.",
        },
        { status: response.status }
      );
    }

    console.log("Polza response:", data);

    return NextResponse.json({
      operationId: data.id,
    });
  } catch (error) {
    console.error("Generation error:", error);

    return NextResponse.json(
      { error: "Не удалось отправить запрос на генерацию." },
      { status: 500 }
    );
  }
}